using System;
public interface IGarminNestedChart
{
	// Default state is the state of the GameObject before user interacts with it, or when its at its original position.
	bool isDefaultState {get; set;}
}
